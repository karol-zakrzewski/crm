import React from "react";

const addDeal = () => {
  return (
    <h2>
      Currently it is not possible to add an offer. This function will be added
      in the future.
    </h2>
  );
};

export default addDeal;
